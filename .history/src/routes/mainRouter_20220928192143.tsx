import { Dashboard, catalogManagerProps, CustomerManager, EmloyeeManager, inventoryManagementProps, ManageOrders, ProductManager, s } from "components/layout";

const PublicRoute = () => [
    { path: '/', component: Dashboard}
];

const PrivateRoute = () => [];

export { PrivateRoute, PrivateRoute };
