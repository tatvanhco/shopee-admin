import { Dashboard, catalogManagerProps, CustomerManager, EmloyeeManager } from "components/layout";

const PublicRoute = () => [
    { path: '/', component: Dashboard}
];

const PrivateRoute = () => [];

export { PrivateRoute, PrivateRoute };
