import { Dashboard, catalogManagerProps, CustomerManager,  } from "components/layout";

const PublicRoute = () => [
    { path: '/', component: Dashboard}
];

const PrivateRoute = () => [];

export { PrivateRoute, PrivateRoute };
