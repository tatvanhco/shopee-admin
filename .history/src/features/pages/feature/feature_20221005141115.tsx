import * as React from 'react';
import styles from './fu.module.scss';

export interface FeatureProps {}

export const Feature = () => {
    return (
    <div className={styles.chars}>
        Chart
    </div>
    );
};
